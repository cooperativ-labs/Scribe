/// Version marker for the host integration package.
///
/// Shared scheduler, handoff, and manifest contracts belong here as they are added
/// by the corresponding objective; the package deliberately has no feature logic.
public enum ScribeAppCore {
    public static let displayName = "Scribe"
}
